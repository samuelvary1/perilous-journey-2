
<div>
	<%= form_tag("/locations/#{@location.id}/pickup") do %>

		<p>
			<%= label_tag(:item, "Enter Item:") %> 
			<%= text_field_tag(:item) %>
		</p>
		
		<%= submit_tag("Pick up!") %>
		
	<% end %>
</div>

<% if flash[:notice] %>
  <p>
    <%= flash[:notice] %>
  </p>
<% end %>
